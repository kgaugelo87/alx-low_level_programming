#include <stdio.h>
/**
 * main -function to print out the exact string provided
 *
 * Return: always return the value (0)
 *
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
