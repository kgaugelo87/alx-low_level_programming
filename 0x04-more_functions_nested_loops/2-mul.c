#include "main.h"

/**
 * mul - multiplying two numbers
 * @a: our fist number
 * @b: our second number
 * Return: always return a value of (0)
 *
 */
int mul(int a, int b)
{
	int total = a * b;

	return (total);
}
